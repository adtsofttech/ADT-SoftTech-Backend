{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @workspace/api-server run build",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 30,
      "includeFiles": "api-server/src/data/**"
    }
  }
}
